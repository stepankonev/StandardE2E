PREFERENCE_TRAJECTORIES_KEY = "preference_trajectories"
INDEX_FILE_NAME = "index.parquet"
# Human-readable per-(dataset, split) descriptor written next to the index by the
# converter: dataset name, split and each adapter's spec (name + metadata, incl.
# BEV grid params / channel orders). Consumed by the visualization tool.
DATASET_INFO_FILE_NAME = "dataset_info.yaml"
